package br.com.keila.modules.fiado.model;

/** Espelha o tipo ENUM `status_fiado` criado em V4__create_clientes_fiado.sql. */
public enum StatusFiado {
    ABERTO,
    PARCIAL,
    PAGO,
    VENCIDO
}
