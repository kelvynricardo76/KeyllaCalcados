package br.com.keila.modules.venda.model;

/** Espelha o tipo ENUM `forma_pagamento` criado em V5__create_caixa_vendas.sql. */
public enum FormaPagamento {
    DINHEIRO,
    DEBITO,
    CREDITO,
    PIX,
    FIADO,
    TROCA,
    OUTRO
}
